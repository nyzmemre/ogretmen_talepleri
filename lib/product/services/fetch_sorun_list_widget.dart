/*
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:flutter/material.dart';
import 'package:group_button/group_button.dart';
import 'package:kartal/kartal.dart';

import '../../core/constant/color_constant.dart';
import '../../core/constant/text_constant.dart';

class FetchSorunListWidget extends StatelessWidget {
  final Function(List<String>) selectedList;
  FetchSorunListWidget({Key? key, required this.selectedList}) : super(key: key);

  List<String>? selectedTitleList;

void newlist(List<String> list) {
  print('çalışıyoooo');
  print(selectedTitleList![0]);
  if(selectedTitleList!=null) {
    print('bura çalıştı');
    selectedList(selectedTitleList!);
  }

}

  @override
  Widget build(BuildContext context) {

    return
  }
}
*/
